#pragma once

// Deklarasi fungsi-fungsi yang akan digunakan dalam program

void reverse(int x);                // Fungsi untuk membalikkan array
void clearArray();                   // Fungsi untuk mengosongkan array
void payload();                      // Fungsi untuk membentuk payload
String toUpperCase(String input);    // Fungsi untuk mengubah string menjadi huruf kapital

void setHome();                      // Fungsi untuk mengatur posisi home
void setPTP(float input);            // Fungsi untuk mengatur posisi Point to Point (PTP)
void getPose();                      // Fungsi untuk mendapatkan posisi
void clearParams();                  // Fungsi untuk mengosongkan parameter
void suctionCupOn();                 // Fungsi untuk menyalakan penyedot
void suctionCupOff();                // Fungsi untuk mematikan penyedot
